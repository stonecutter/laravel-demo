--- vendor/tcg/voyager/src/Traits/Spatial.php	2018-10-22 00:15:14.000000000 +0800
+++ sinkcup/tcg/voyager/src/Traits/Spatial.php	2018-12-18 20:32:38.252556920 +0800
@@ -15,6 +15,9 @@
      */
     public function getLocation($column)
     {
+        if (is_array($column)) {
+            return "POINT({$column['lng']} {$column['lat']})";
+        }
         $model = self::select(DB::raw('ST_AsText('.$column.') AS '.$column))
             ->where('id', $this->id)
             ->first();
@@ -33,15 +36,11 @@
 
         if (!empty($this->spatial)) {
             foreach ($this->spatial as $column) {
-                $clear = trim(preg_replace('/[a-zA-Z\(\)]/', '', $this->getLocation($column)));
-                if (!empty($clear)) {
-                    foreach (explode(',', $clear) as $point) {
-                        list($lng, $lat) = explode(' ', $point);
-                        $coords[] = [
-                            'lat' => $lat,
-                            'lng' => $lng,
-                        ];
-                    }
+                if (!empty($this->{$column})) {
+                    $coords[] = [
+                        'lat' => $this->{$column}['lat'],
+                        'lng' => $this->{$column}['lng'],
+                    ];
                 }
             }
         }
